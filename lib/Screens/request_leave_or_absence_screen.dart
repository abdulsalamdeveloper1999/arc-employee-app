import 'package:flutter/Material.dart';

class RequestLeaveOrAbsence extends StatelessWidget {
  const RequestLeaveOrAbsence({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
